Выполняю п. 1. п. 2.. п.3.
п. 4. - не выполняю.
п. 5 - не выполняю.
Ошибки:
```
Description	Resource	Path	Location	Type
Syntax error, insert "}" to complete Statement	MainWindow.java	/SqText/src/com/sqtext/gui	line 425	Java Problem
Syntax error, insert "else Statement" to complete IfStatement	MainWindow.java	/SqText/src/com/sqtext/gui	line 425	Java Problem
The method compressDouble() is undefined for the type MainWindow	MainWindow.java	/SqText/src/com/sqtext/gui	line 308	Java Problem
The method decompressBinary(byte[]) is undefined for the type AdvancedCompressor	AdvancedCompressor.java	/SqText/src/com/sqtext/compression	line 250	Java Problem
The method decompressBinary(byte[]) is undefined for the type AdvancedCompressor	AdvancedCompressor.java	/SqText/src/com/sqtext/compression	line 265	Java Problem
```
Фрагменты кода:
```
double ratio = (double) compressedSize / decompressedSize;

long time = endTime - startTime;

outputArea.setText(decompressed);

String stats = String.format("✅ Распаковано: %d → %d байт (коэфф.: %.2f) за %d мс",

compressedSize, decompressedSize, ratio, time);

statsLabel.setText(stats);

statsLabel.setForeground(new Color(0, 150, 0));

// Сохраняем в историю

HistoryManager.HistoryEntry entry = new HistoryManager.HistoryEntry(

"DECOMPRESS", compressedSize, decompressedSize, time);

HistoryManager.addEntry(entry);

} catch (Exception e) {

statsLabel.setText("❌ Ошибка распаковки: " + e.getMessage());

statsLabel.setForeground(Color.RED);

e.printStackTrace();

}

}

}
```

```
outputArea.setText(decompressed);

String stats = String.format("✅ Распаковано: %d → %d байт (коэфф.: %.2f) за %d мс",

compressedSize, decompressedSize, ratio, time);

statsLabel.setText(stats);

statsLabel.setForeground(new Color(0, 150, 0));

// Сохраняем в историю

HistoryManager.HistoryEntry entry = new HistoryManager.HistoryEntry(

"DECOMPRESS", compressedSize, decompressedSize, time);

HistoryManager.addEntry(entry);

} catch (Exception e) {

statsLabel.setText("❌ Ошибка распаковки: " + e.getMessage());

statsLabel.setForeground(Color.RED);

e.printStackTrace();

}

}

}
```

```
if ("Smart (автовыбор)".equals(algorithm)) {

smartCompress();

return;

}

if ("Двойное сжатие (DEFLATE+GZIP)".equals(algorithm)) {

compressDouble();

return;

}

  

int depth = (Integer) depthSpinner.
```

```
String base64 = json.substring(start, end);

// Декодируем Base64

byte[] data = Base64.getDecoder().decode(base64);

// Пробуем распаковать как GZIP

try {

return decompressBinary(data);

} catch (IOException e1) {

// Если не GZIP, пробуем как DEFLATE + GZIP

try {

ByteArrayOutputStream baos = new ByteArrayOutputStream();

ByteArrayInputStream bais = new ByteArrayInputStream(data);

try (java.util.zip.InflaterInputS
```

```
// Пробуем распаковать как GZIP

try {

return decompressBinary(data);

} catch (IOException e1) {

// Если не GZIP, пробуем как DEFLATE + GZIP

try {

ByteArrayOutputStream baos = new ByteArrayOutputStream();

ByteArrayInputStream bais = new ByteArrayInputStream(data);

try (java.util.zip.InflaterInputStream inflaterIn =

new java.util.zip.InflaterInputStream(bais)) {

byte[] buffer = new byte[1024];

int len;

while ((len = inflaterIn.read(buffer)) != -1) {

baos.write(buffer, 0, len);

}

}

byte[] gzipData = baos.toByteArray();

return decompressBinary(gzipData);

} catch (IOException e2) {

throw new IOException("Не удалось распаковать ultra-compact формат");

}

}

}

}
```